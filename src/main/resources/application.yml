#配置資料庫
spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
  resources:
    static-locations: classpath:/ #只要是static裡面的資源，要從根目錄開始找 ?
